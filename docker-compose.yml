version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: swarm_postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: [REDACTED-DB-PASSWORD]
      POSTGRES_DB: swarm_leads
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
